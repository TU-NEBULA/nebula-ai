version: '3.8'

services:
  celery:
    image: nebula-fastapi
    container_name: celery-dev
    build:
      context: .
      dockerfile: Dockerfile
    command: celery -A app.worker worker --loglevel=info
    env_file:
      - .env
    depends_on:
      - rabbitmq
      - redis
